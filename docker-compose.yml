version: '3.8'

services:
  # Base de données PostgreSQL 16
  postgres:
    image: postgres:16-alpine
    container_name: outfitera-postgres
    restart: always
    environment:
      POSTGRES_DB: outfitera_db
      POSTGRES_USER: outfitera_user
      POSTGRES_PASSWORD: outfitera_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - outfitera-network

  # Interface d'administration PgAdmin 4
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: outfitera-pgadmin
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@outfitera.com
      PGADMIN_DEFAULT_PASSWORD: adminpassword
    ports:
      - "5050:80"
    depends_on:
      - postgres
    networks:
      - outfitera-network

  # Service Backend Spring Boot
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: outfitera-backend
    restart: always
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=dev
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=outfitera_db
      - DB_USER=outfitera_user
      - DB_PASSWORD=outfitera_password
    depends_on:
      - postgres
    networks:
      - outfitera-network

networks:
  outfitera-network:
    driver: bridge

volumes:
  postgres_data:
    driver: local
